generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Student {
  roll_no String @id
  name    String
  dob     String
  class   String @default("")
}
